# 让 Grafana 启动时自动从目录加载仪表盘 JSON
apiVersion: 1

providers:
  - name: 'poker'
    orgId: 1
    folder: 'Poker'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
