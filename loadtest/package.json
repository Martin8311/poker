{
  "name": "poker-loadtest",
  "version": "1.0.0",
  "private": true,
  "description": "Load test scripts for the poker game (HTTP throughput + WebSocket/STOMP concurrency & broadcast latency)",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
